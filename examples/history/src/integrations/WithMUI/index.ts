/**
 * Material-UI (MUI) Integration
 *
 * Components using Material-UI's component library.
 * These components wrap the headless timeline components with MUI styling.
 */

export { MUITimeline } from './Timeline';
export { MUIControls } from './Controls';
